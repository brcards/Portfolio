import styled from 'styled-components';

const CardQuickInfo = styled.div`
    position: absolute;
    display: flex;
    align-items: center;
    top: 0;
    right: 0;
    font-size: 1.3em;
    > * {
        margin: 0.5em;
    }
`;

export default CardQuickInfo;
